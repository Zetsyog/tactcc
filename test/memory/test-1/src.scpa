(* Ce programme à pour but de tester la libération
de la mémoire lors d'une mauvaise syntaxe dans la déclaration d'une fonction *)

program test
    var i,j: int;
    var b1, b2, b3: bool
    function f(a: int, b: bool): int;
        begin
            if(b) then
                write a;
            write "\n";
        end;
    begin
        read i;
        read k;
        if i <> j then
            write "diff\n";
    end
        
