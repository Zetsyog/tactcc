program test
    var i,j: int;
    var b1, b2, b3: bool
    function f(a: int, b: bool): int;<--syntax error
        begin
            if(b) then
                write a;
            write "\n";
        end;
    begin
        read i;
        read k;
        if i <> j then
            write "diff\n";
    end
        
