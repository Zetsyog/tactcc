(* Ce programme vérifie que la structure de contrôle "while do" est fonctionnelle *)

program test
    var b2: int
    begin
        b2 := 1;
        while b2 == 1 do
        begin
            write b2 ;
            b2 := 2;
        end;
        write "\n";
    end
