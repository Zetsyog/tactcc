(* Ce programme vérifie que l'inégalité supérieure large est bien implémentée *)

program test
    var i,j: int
    begin
        i := 8;
        j := 9;
	    if j >= i then 
        	write j;
        write "\n";
        write "Fin du programme\n";
    end
