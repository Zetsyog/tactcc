(* Ce programme vérifie le bon fonctionnement de l'opérateur de comparaison "<>" est fonctionnel *)

program test
    var i,j: int
    begin
        i := 8;
        j := 9;
        if i <>j then
                write i;
        write "\n";
        write "Fin du programme\n";
    end
