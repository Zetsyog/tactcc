program test
    var a, b, c: int
    function j(p1: int, p2: bool): unit
        var c, d: int
        begin
            write p1;
        end;
    begin
        write "hello world\n";
        j(5, true);
        write "\n";
    end
